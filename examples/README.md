# Examples

| Example | Style | What it shows |
|---|---|---|
| [`resume/`](./resume/) | closure | **Start here** — crash recovery and step replay |
| [`func-task/`](./func-task/) | closure | `durable.Func` inline tasks, step output chaining |
| [`struct-task/`](./struct-task/) | struct | Injected dependencies, `WithTimeout`, `WithMaxRetries` |
