coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: 40%
        threshold: 5%

# Paths excluded from coverage reports.
ignore:
  - "examples/**"
  - "benchmarks/**"
  - "**/*_test.go"
